= render layout: 'form_wrapper', locals: { heading: 'Anleitung zum Entsperren erneut senden',
  intro_text: "Gib deine E-Mail-Adresse ein, um den Link zum Entsperren des Kontos zu erhalten." } do

  = form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f|

    = render 'devise/shared/error_messages', resource: resource

    = render layout: 'field_wrapper' do
      = f.label :email
      = f.email_field :email, autofocus: true, autocomplete: 'email', class: 'form-control'

    = render layout: 'field_wrapper', locals: { pt: 'pt-2' } do
      = f.submit 'Anleitung zum Entsperren erneut senden', class: 'btn btn-primary'

    = render 'devise/shared/links'
