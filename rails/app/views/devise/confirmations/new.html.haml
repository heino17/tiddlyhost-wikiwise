= render layout: 'form_wrapper', locals: { heading: 'Anleitung zur Bestaetigung erneut senden',
  intro_text: "Gib deine E-Mail-Adresse ein, um den Bestaetigungslink erneut zu erhalten." } do

  = form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f|

    = render 'devise/shared/error_messages', resource: resource

    = render layout: 'field_wrapper' do
      = f.label :email
      = f.email_field :email, autofocus: true, autocomplete: 'email', class: 'form-control',
        value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email)

    = render layout: 'field_wrapper', locals: { pt: 'pt-2' } do
      = f.submit 'Anleitung zur Bestaetigung erneut senden', class: 'btn btn-primary'

    = render 'devise/shared/links'
